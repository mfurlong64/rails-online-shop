
= form_for @product, {:action => "/admin", :id => "new_admin", :controller => 'admin'}  do |f|
  - if @product.errors.any?
    - @product.errors.full_messages.each do |msg|
      %li
        =msg
  %p
    = f.label :name
    %br
    = f.text_field :name
  %p
    = f.label :photo
    %br
    = f.text_field :photo
  %p
    = f.label :description
    %br
    = f.text_area :description
  %p
    = f.label :price
    %br
    = f.text_field :price
  %p
    = f.label :stock
    %br
    = f.text_field :stock
  =fields_for @category do |b|
    /* %div Add to Category
    /*- send_categories.each do |category|
    /*  %br
    /*    = b.label category.id, category.name
    /*    = b.check_box_tag :"#{category.id}"
    %p
      =b.label "Add to Category"
      %br
      =b.text_field :name
      %div Current Categories
      - send_categories.each do |category|
        = "- #{category.name}"
        %br
  %p= f.submit













