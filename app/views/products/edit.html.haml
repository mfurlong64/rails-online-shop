%h1 New Product
%p Please add your new product

= form_for :product, url: products_path(@product), method: :patch do |f|
  - if @product.errors.any?
    %li
      - @product.errors.full_messages.each do |msg|
        =msg
  %p
    = f.label :name
    %br
    = f.text_field :name
  %p
    = f.label :description
    %br
    = f.text_area :description
  %p= f.submit
